-- cfgId                            int                              ID
-- expression                       string                           
-- minMit                           int                              MIT
-- maxMit                           int                              MIT

return {
	[102] = {
		cfgId = 102,
		expression = "return function(mit) return (mit +1000) / 1000 end",
		minMit = 0,
		maxMit = 200,
	},
	[103] = {
		cfgId = 103,
		expression = "return function(mit) return (mit +1000) / 1000 end",
		minMit = 0,
		maxMit = 200,
	},
	[104] = {
		cfgId = 104,
		expression = "return function(mit) return (mit +1000) / 1000 end",
		minMit = 0,
		maxMit = 200,
	},
	[105] = {
		cfgId = 105,
		expression = "return function(mit) return (mit +1000) / 1000 end",
		minMit = 0,
		maxMit = 200,
	},
	[106] = {
		cfgId = 106,
		expression = "return function(mit) return (mit +1000) / 1000 end",
		minMit = 0,
		maxMit = 200,
	},
}
